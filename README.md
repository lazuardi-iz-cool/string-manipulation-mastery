# JavaScript String Laboratory 🧪

Kumpulan modul latihan intensif untuk menguasai manipulasi String di JavaScript. Repositori ini mencakup teknik dasar hingga transformasi teks yang lebih kompleks.

## 📂 Daftar Eksperimen
1.  **Penggabungan**: Teknik `+` vs Template Literals.
2.  **Panjang String**: Mengukur jumlah karakter dengan `.length`.
3.  **Akses Karakter**: Mengambil karakter berdasarkan indeks.
4.  **Mengubah Huruf**: Transformasi `.toUpperCase()` dan `.toLowerCase()`.
5.  **Mengambil Potongan**: Penggunaan `.slice()`, `.substring()`, atau `.substr()`.
6.  **Mencari Isi String**: Mencari posisi teks dengan `.indexOf()` atau `.includes()`.
7.  **Mengganti Teks**: Manipulasi isi string menggunakan `.replace()`.
8.  **Memecah String**: Mengubah string menjadi array dengan `.split()`.
9.  **Menghapus Spasi**: Membersihkan spasi di ujung teks dengan `.trim()`.
10. **Konversi ke String**: Mengubah tipe data lain menjadi string menggunakan `.toString()`.

## 🚀 Cara Penggunaan
Setiap folder berisi file `index.html`. Cukup buka file tersebut di browser untuk melihat output JavaScript-nya melalui Console (F12) atau tampilan antarmuka yang tersedia.

---
*Dibuat dengan ❤️ untuk dokumentasi belajar JavaScript.*
